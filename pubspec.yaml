name: perfectpick_wa
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.19.4 <4.0.0"

dependencies:
  dartdap: ^0.7.1
  english_words: ^4.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.5
  flutter_dotenv: ^5.1.0
  flutter_secure_storage: ^9.0.0
  graphql_flutter: ^5.1.2
  provider: ^6.1.2
  test: ^1.24.9
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - lib/presentation/images/
    - .env
